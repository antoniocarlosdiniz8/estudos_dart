void main(List<String> arguments) {
  //Variável
  double nota = 7;
  //Variável que recebe o resultado da operação
  String nome = nota >= 6 ? 'Aprovado' : 'Reprovado';//Operador ternário na pratica
  print(nome);//Saída do resultado
}
